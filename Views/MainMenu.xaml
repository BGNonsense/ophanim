<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:pmrk="clr-namespace:Playnite.FullscreenApp.Markup;assembly=Playnite.FullscreenApp">

    <Style x:Key="MainMenuBorder" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource OverlayMenuBackgroundBrush}" />
    </Style>

    <Style x:Key="MainMenuButton" BasedOn="{StaticResource {x:Type ButtonEx}}" TargetType="ButtonEx">
        <Setter Property="Margin" Value="0,5,0,5" />
        <Setter Property="Padding" Value="20,3,20,3" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
    </Style>

    <DataTemplate x:Key="MainMenuHeaderTemplate">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="20,30,40,30">
            <Image Source="{pmrk:ThemeFile 'Images/applogo_clean.png'}" Height="48"
                   RenderOptions.BitmapScalingMode="Fant" />
            <Image Height="24" Source="{ThemeFile 'Resources/blank.png'}" Stretch="Fill" Width="24" RenderTransformOrigin="0.5,0.5">
                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="-1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Image.RenderTransform>
            </Image>
            <Image Height="78.312" Source="{ThemeFile 'Resources/poweredbyplaynite.png'}" Stretch="Fill" Width="187.892"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuSettingsButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/options.png'}" Stretch="Fill" Width="312.432"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuLibUpdateButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="49.98" Source="{ThemeFile 'Resources/refreshlibrary.png'}" Stretch="Fill" Width="304.029"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuCancelLibUpdateButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/cancelrefresh.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuExitPlayniteButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/exitplaynite.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuSwithDesktopButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/desktop.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuRandomGameButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/surprise me.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuShutdowButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/shutdown.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuSuspendButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/suspend.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuHibernateButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/hibernate.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuRestartButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/REBOOT.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuHelpButtonTemplate">

        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/HELP.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuMinimizeButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/MINIMIZE.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuLockSystemButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/LOCK.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuLogoutUserButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/LOGOUT.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="MainMenuClientsButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Height="50" Source="{ThemeFile 'Resources/gamestores.png'}" Stretch="Fill" Width="312.43"/>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>